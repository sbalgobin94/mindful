<h1>Here are all your goals</h1>

<h2> <%= link_to "New Goal", new_goal_path %>

<ul>
    <% @goals.each do |goal| %>
        <li>
            <%= link_to "#{goal.content} | #{goal.status}", goal_path(goal) %>
        </li>
    <% end %>
</ul>